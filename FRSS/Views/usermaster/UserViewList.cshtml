@using Business
@using FRSS.Utility
@model IEnumerable<FRSS.Models.usermastermodel>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<style>
    .dropdown-menu > li > a:focus, .dropdown-menu > li > a:hover {
        background-color: #008d4c;
        color: white;
    }

    .dropdown-menu > li > a {
        color: white;
    }
</style>

<!-- Content Header (Page header) -->
<section class="content-header">
    <h1>
        User Master
    </h1>
</section>

<!-- Main content -->
<section class="content">
    <div class="row">
        <div class="col-xs-12">
            <!-- /.box -->
            <div class="box">
                <div class="box-header">
                    @Html.Partial("~/Views/Shared/_ErrorMessage.cshtml")
                    <div class="col-md-8">
                        &nbsp;
                    </div>
                    <div class="col-md-2 btn-padding-right">
                        @if (ProjectSession.addrights == true)
                        {
                            <a href="@(Url.Action("formusermaster", "usermaster"))" class="btn btn-block btn-info"><i class="fa fa-plus"></i> Add </a>
                        }
                    </div>
                </div>
                <!-- /.box-header -->
                <div class="box-body">
                    <table class="table table-bordered table-striped" id="example1">
                        <thread>
                            <tr>
                                <td>
                                    @Html.DisplayNameFor(model => model.userid)
                                </td>
                                <td>
                                    @Html.DisplayNameFor(model => model.username)
                                </td>
                                <td>
                                    @Html.DisplayNameFor(model => model.usermobile)
                                </td>
                                <td>
                                    @Html.DisplayNameFor(model => model.useremail)
                                </td>
                                <td>
                                    @Html.DisplayNameFor(model => model.userstatus)
                                </td>
                                <td>Action</td>
                            </tr>
                        </thread>

                        @foreach (usermasterlist item in (List<usermasterlist>)ViewBag.usermasterList)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.userid)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.username)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.usermobile)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.useremail)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.userstatus)
                                </td>
                                <td>
                                    @if (ProjectSession.editrights == true)
                                    {
                                        <a href="~/usermaster/formusermaster?id=@item.userid" title="Edit"><span class="glyphicon glyphicon-edit"></span></a>
                                    }
                                </td>
                            </tr>

                        }

                    </table>
                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div>
    <!-- /.row -->
</section>
<!-- /.content -->
<!-- /.modal -->
<script>
    $(function () {
        $('#example1').DataTable({
            'paging': false,
            'lengthChange': false,
            'searching': true,
            'ordering': true,
            'info': false,
            'autoWidth': false
        });

        $(".sidebar-menu li").removeClass('active');
        $("#limaster").addClass("active");
        $("#liusermaster").addClass("active");
    })
</script>
